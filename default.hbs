<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{meta_title}}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,500;1,6..72,400;1,6..72,500&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="{{asset "css/screen.css"}}">

    <style>
        /* 1. Base light mode colors (default) */
        :root {
            --color-bg: #FAF7ED;
            --color-text: #000000;
            --color-text-secondary: #444;
            --color-accent: #915109;
            --color-border: rgba(0,0,0,0.1);
            --color-link: #1a5f7a;
            --color-link-hover: #915109;
            --font-body: 'Newsreader', Georgia, serif;
            --font-ui: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            --font-mono: 'SF Mono', Menlo, monospace;
            --content-width: 38rem;
            --spacing-unit: 1rem;
        }

        /* 2. System dark preference (when no manual override) */
        @media (prefers-color-scheme: dark) {
            html:not(.light-mode) {
                --color-bg: #1f1e1b;
                --color-text: #e8e6e0;
                --color-text-secondary: #a0a0a0;
                --color-accent: #d4a574;
                --color-border: rgba(255,255,255,0.1);
                --color-link: #7eb8c9;
                --color-link-hover: #d4a574;
            }
        }

        /* 3. Manual dark toggle (explicit preference) */
        html.dark-mode {
            --color-bg: #1f1e1b;
            --color-text: #e8e6e0;
            --color-text-secondary: #a0a0a0;
            --color-accent: #d4a574;
            --color-border: rgba(255,255,255,0.1);
            --color-link: #7eb8c9;
            --color-link-hover: #d4a574;
        }

        /* 4. Manual light toggle (overrides system dark) */
        html.light-mode {
            --color-bg: #FAF7ED;
            --color-text: #000000;
            --color-text-secondary: #444;
            --color-accent: #915109;
            --color-border: rgba(0,0,0,0.1);
            --color-link: #1a5f7a;
            --color-link-hover: #915109;
        }

        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }

        html {
            font-size: 18px;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: var(--font-body);
            font-size: 1rem;
            line-height: 1.7;
            color: var(--color-text);
            background-color: var(--color-bg);
            transition: background-color 0.3s ease, color 0.3s ease;
            hyphens: auto;
            -webkit-hyphens: auto;
            -ms-hyphens: auto;
        }

        a {
            color: var(--color-link);
            text-decoration: underline;
            text-decoration-thickness: 1px;
            text-underline-offset: 2px;
            transition: color 0.2s ease;
        }

        a:hover {
            color: var(--color-link-hover);
        }

        .site-wrapper {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .site-header {
            padding: 1.5rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .site-header a {
            text-decoration: none;
            color: var(--color-text-secondary);
            transition: color 0.2s ease;
        }

        .site-header a:hover {
            color: var(--color-text);
        }

        .site-header svg {
            width: 20px;
            height: 20px;
            stroke: currentColor;
            fill: none;
        }

        .site-main {
            flex: 1;
            width: 100%;
            max-width: var(--content-width);
            margin: 0 auto;
            padding: 2rem;
        }

        /* Post and Page typography */
        .post-header, .page-header {
            margin-bottom: 2rem;
        }

        .post-title, .page-title {
            font-size: 2rem;
            font-weight: 500;
            line-height: 1.2;
            margin: 0 0 0.75rem 0;
            letter-spacing: -0.02em;
        }

        .post-meta {
            font-size: 0.9rem;
            color: var(--color-text-secondary);
            font-family: var(--font-ui);
        }

        .unpolished-tag {
            color: var(--color-accent);
        }

        /* Post image */
        .post-image {
            margin: 0 0 2rem 0;
        }

        .post-image img {
            width: 100%;
            height: auto;
            border-radius: 4px;
        }

        .post-image figcaption {
            font-size: 0.85rem;
            color: var(--color-text-secondary);
            text-align: center;
            margin-top: 0.5rem;
            font-style: italic;
        }

        /* Content styling */
        .gh-content {
            font-size: 1.05rem;
            line-height: 1.75;
            hyphens: auto;
            -webkit-hyphens: auto;
            -ms-hyphens: auto;
        }

        .gh-content > * + * {
            margin-top: 1.5rem;
        }

        .gh-content p {
            margin: 0;
        }

        .gh-content > p + p {
            margin-top: 1.25rem;
        }

        /* Drop cap */
        .gh-content > p:first-of-type::first-letter {
            float: left;
            font-size: 3.25rem;
            line-height: 1;
            font-weight: 500;
            margin-right: 0.5rem;
            margin-top: 0.1rem;
            color: var(--color-accent);
        }

        /* Headings in content */
        .gh-content h2 {
            font-size: 1.4rem;
            font-weight: 500;
            margin-top: 2.5rem;
            margin-bottom: 0.75rem;
        }

        .gh-content h3 {
            font-size: 1.2rem;
            font-weight: 500;
            margin-top: 2rem;
            margin-bottom: 0.5rem;
        }

        /* Lists - with proper spacing for numbers */
        .gh-content ul,
        .gh-content ol {
            padding-left: 1.75rem;
            margin: 1.25rem 0;
        }

        .gh-content li {
            margin-bottom: 0.5rem;
            padding-left: 0.25rem;
        }

        /* Blockquote */
        .gh-content blockquote {
            padding-left: 1.25rem;
            margin: 1.5rem 0;
            color: var(--color-text-secondary);
        }

        .gh-content blockquote p {
            margin: 0;
        }

        /* Code */
        .gh-content code {
            font-family: var(--font-mono);
            font-size: 0.9em;
            background: var(--color-border);
            padding: 0.15em 0.4em;
            border-radius: 3px;
        }

        .gh-content pre {
            background: var(--color-border);
            padding: 1.25rem;
            border-radius: 6px;
            overflow-x: auto;
            margin: 1.5rem 0;
        }

        .gh-content pre code {
            background: none;
            padding: 0;
            font-size: 0.875rem;
            line-height: 1.6;
        }

        /* Images and figures */
        .gh-content figure {
            margin: 1.5rem 0;
        }

        .gh-content img {
            max-width: 100%;
            height: auto;
            border-radius: 4px;
        }

        .gh-content figcaption {
            font-size: 0.85rem;
            color: var(--color-text-secondary);
            text-align: center;
            margin-top: 0.5rem;
            font-style: italic;
        }

        /* Horizontal rule */
        .gh-content hr {
            border: none;
            border-top: 1px solid var(--color-border);
            margin: 2.5rem 0;
        }

        /* Post navigation */
        .post-nav {
            display: flex;
            justify-content: space-between;
            gap: 1rem;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid var(--color-border);
            font-family: var(--font-ui);
            font-size: 0.9rem;
        }

        .post-nav a {
            color: var(--color-text-secondary);
            text-decoration: none;
            max-width: 45%;
        }

        .post-nav a:hover {
            color: var(--color-link);
        }

        /* Footer */
        .site-footer {
            padding: 2rem;
            text-align: center;
            font-size: 0.85rem;
            color: var(--color-text-secondary);
            border-top: 1px solid var(--color-border);
        }

        .site-footer nav {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 1.5rem;
            flex-wrap: wrap;
        }

        .site-footer a {
            color: var(--color-text-secondary);
            text-decoration: none;
        }

        .site-footer a:hover {
            color: var(--color-text);
        }

        /* Dark mode toggle */
        .dark-mode-toggle {
            background: none;
            border: none;
            cursor: pointer;
            padding: 0;
            color: var(--color-text-secondary);
            display: flex;
            align-items: center;
        }

        .dark-mode-toggle:hover {
            color: var(--color-text);
        }

        .dark-mode-toggle svg {
            width: 18px;
            height: 18px;
            stroke: currentColor;
            fill: none;
        }

        .dark-mode-toggle .icon-sun {
            display: none;
        }

        /* Show sun icon in dark mode (manual toggle) */
        html.dark-mode .dark-mode-toggle .icon-sun {
            display: block;
        }

        html.dark-mode .dark-mode-toggle .icon-moon {
            display: none;
        }

        /* Show sun icon when system prefers dark (and no light override) */
        @media (prefers-color-scheme: dark) {
            html:not(.light-mode) .dark-mode-toggle .icon-sun {
                display: block;
            }

            html:not(.light-mode) .dark-mode-toggle .icon-moon {
                display: none;
            }
        }

        /* Hide code injection floating button */
        .theme-switch-button {
            display: none !important;
        }

        /* Archive pages (tag, author, index) */
        .tag-header, .author-header, .archive-header {
            margin-bottom: 2rem;
        }

        .tag-title, .author-name, .archive-title {
            font-size: 1.75rem;
            font-weight: 500;
            margin: 0 0 0.5rem 0;
        }

        .tag-description, .author-bio {
            color: var(--color-text-secondary);
            font-size: 1rem;
            margin: 0;
        }

        /* Post list (used on home, tag, author, archive) */
        .post-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .post-item {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            gap: 1rem;
            padding: 0.5rem 0.5rem;
            border-radius: 4px;
            transition: background-color 0.15s ease;
        }

        .post-item:hover {
            background-color: rgba(0,0,0,0.03);
        }

        @media (prefers-color-scheme: dark) {
            html:not(.light-mode) .post-item:hover {
                background-color: rgba(255,255,255,0.05);
            }
        }

        html.dark-mode .post-item:hover {
            background-color: rgba(255,255,255,0.05);
        }

        .post-item-title {
            flex: 1;
            min-width: 0;
        }

        .post-item-title a {
            color: var(--color-text);
            text-decoration: none;
            transition: color 0.2s ease;
        }

        .post-item-title a:hover {
            color: var(--color-link);
        }

        .post-item-date {
            font-size: 0.85rem;
            color: var(--color-text-secondary);
            font-family: var(--font-ui);
            white-space: nowrap;
        }

        /* Pagination */
        .pagination {
            display: flex;
            justify-content: space-between;
            gap: 1rem;
            margin-top: 2.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--color-border);
            font-family: var(--font-ui);
            font-size: 0.9rem;
        }

        .pagination a {
            color: var(--color-text-secondary);
            text-decoration: none;
        }

        .pagination a:hover {
            color: var(--color-link);
        }

        /* Home page specific */
        .home-header {
            margin-bottom: 2.5rem;
        }

        .site-title {
            font-size: 2rem;
            font-weight: 500;
            margin: 0 0 0.25rem 0;
        }

        .site-title a {
            color: inherit;
            text-decoration: none;
        }

        .site-description {
            color: var(--color-text-secondary);
            font-size: 1rem;
            font-style: italic;
            margin: 0;
        }

        .home-section {
            margin-bottom: 2.5rem;
        }

        .section-title {
            font-size: 0.8rem;
            font-family: var(--font-ui);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--color-text-secondary);
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid var(--color-border);
        }

        .about-text {
            font-size: 1.05rem;
            line-height: 1.75;
        }

        .view-all {
            display: inline-block;
            margin-top: 0.75rem;
            font-size: 0.9rem;
            color: var(--color-link);
            text-decoration: none;
            font-family: var(--font-ui);
        }

        .view-all:hover {
            text-decoration: underline;
        }

        /* Error page */
        .error-page {
            text-align: center;
            padding-top: 4rem;
        }

        .error-code {
            font-size: 5rem;
            font-weight: 300;
            color: var(--color-text-secondary);
            margin: 0;
        }

        .error-message {
            font-size: 1.25rem;
            color: var(--color-text-secondary);
            margin: 0.5rem 0 2rem 0;
        }

        .error-link {
            font-family: var(--font-ui);
        }

        /* Responsive */
        @media (max-width: 600px) {
            html {
                font-size: 16px;
            }

            .site-header {
                padding: 1rem 1.25rem;
            }

            .site-main {
                padding: 1.5rem 1.25rem;
            }

            .post-title, .page-title {
                font-size: 1.6rem;
            }

            .tag-title, .author-name, .archive-title {
                font-size: 1.4rem;
            }

            .site-title {
                font-size: 1.5rem;
            }

            .gh-content > p:first-of-type::first-letter {
                font-size: 2.75rem;
            }

            .post-nav {
                flex-direction: column;
            }

            .post-nav a {
                max-width: 100%;
            }

            .post-item {
                flex-direction: column;
                gap: 0.25rem;
                align-items: flex-start;
            }

            .post-item-date {
                font-size: 0.8rem;
            }

            .section-title {
                font-size: 0.75rem;
            }
        }
    </style>

    {{ghost_head}}
</head>
<body class="{{body_class}}">
    <div class="site-wrapper">
        <header class="site-header">
            <a href="{{@site.url}}" title="Home">
                {{> icons/home}}
            </a>
            <a href="#/search" title="Search">
                {{> icons/search}}
            </a>
        </header>

        {{{body}}}

        <footer class="site-footer">
            <nav>
                <a href="#/subscribe">Subscribe</a>
                <a href="/write-to-me/">Write to me</a>
                <button class="dark-mode-toggle" aria-label="Toggle dark mode">
                    <span class="icon-moon">{{> icons/moon}}</span>
                    <span class="icon-sun">{{> icons/sun}}</span>
                </button>
            </nav>
        </footer>
    </div>

    <script src="{{asset "js/main.js"}}"></script>
    {{ghost_foot}}
</body>
</html>
